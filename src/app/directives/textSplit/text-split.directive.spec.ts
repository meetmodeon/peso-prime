import { TextSplitDirective } from './text-split.directive';

describe('TextSplitDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSplitDirective();
    expect(directive).toBeTruthy();
  });
});
