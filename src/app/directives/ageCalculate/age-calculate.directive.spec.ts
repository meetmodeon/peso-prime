import { AgeCalculateDirective } from './age-calculate.directive';

describe('AgeCalculateDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeCalculateDirective();
    expect(directive).toBeTruthy();
  });
});
